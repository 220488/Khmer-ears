import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#B8DDFF",
      main: "#1D3746"
    },
    secondary: {
      light: "#B8DDFF",
      main: "#D2BD00"
    }
  }
})

export default theme